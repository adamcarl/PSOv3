<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sydney.psov3">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/pos_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AdminActivity" android:label="Admin Priveleges">
        </activity>

        <activity android:name=".Cashier"></activity>

        <activity android:name=".ManageStaff"
            android:parentActivityName=".AdminActivity"
            android:label="Manage Staff"/>

        <activity android:name=".ManageProduct"
            android:parentActivityName=".AdminActivity"
            android:label="Manage Product">
        </activity>

        <activity android:name=".ManageJournal"
            android:parentActivityName=".AdminActivity"
            android:label="Manage Journal"/>

        <activity android:name=".ManageProfile"
            android:parentActivityName=".AdminActivity"
            android:label="My Profile"/>



    </application>

</manifest>